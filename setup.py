from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='this is a project to analysis the comments',
    author='neeraj',
    license='',
)
